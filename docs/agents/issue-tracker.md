# Issue Tracker

This repository uses GitHub Issues at `Lychee-Technology/LTSearch`.

Use the `gh` CLI for issue creation, inspection, and updates. Create ready-to-implement work with
the `ready-for-agent` label unless a human decision is required.
